{
  "name": "run-ejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "ejs-cli --base-dir src '*.ejs' --out dist/",
    "ejs": "ejs-cli --base-dir src '*.ejs' --out dist/",
    "watch:ejs": "watch 'npm run ejs' src",
    "bs": "browser-sync start --server 'dist' --files='dist/*.html' --port 10000 --ui-port 10001"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "ejs-cli": "^2.1.1",
    "npm-run-all": "^4.1.5",
    "watch": "^1.0.2"
  },
  "dependencies": {
    "express": "^3.3.8"
  }
}
